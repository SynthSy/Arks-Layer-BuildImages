# Arks-Layer-BuildImages

A custom Docker image running Alpine that we use for our spelling/CSV checking when submitting translations. 

Our initial one used CircleCI's python3.11-node image which was Debian based.